export const isDisabled = (myObject: any) => {
  // let disabled = false
  // Object.entries(myObject).forEach(([key, value]) => {
  //   if (value == '') {
  //     disabled = true
  //     return 0
  //   }
  // })
  // return disabled
  return false
}